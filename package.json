{
  "name": "s3ify",
  "version": "1.0.0",
  "description": "Reads from a web service and writes to s3",
  "main": "cli.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "leilers",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.117.0",
    "request": "^2.81.0",
    "request-promise-native": "^1.0.4"
  }
}
